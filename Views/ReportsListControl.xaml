<UserControl x:Class="Winton.Views.ReportsListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="{DynamicResource SurfaceBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- REPORT LIST CARD -->
        <Border Background="{DynamicResource CardBackground}"
                CornerRadius="10"
                BorderBrush="{DynamicResource Divider}"
                BorderThickness="1"
                Margin="15"
                Padding="10">
            <Border.Effect>
                <DropShadowEffect Color="#33000000"
                                  BlurRadius="20"
                                  ShadowDepth="0"/>
            </Border.Effect>

            <ListView x:Name="lstReports"
                      MouseDoubleClick="LstReports_MouseDoubleClick"
                      Background="Transparent"
                      BorderThickness="0"
                      Foreground="{DynamicResource PrimaryText}">

                <!-- Modern ListViewItem style -->
                <ListView.Resources>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}" />
                        <Setter Property="Padding" Value="12,6" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />

                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#11000000"/>
                            </Trigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#22000000"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Resources>

                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Report Date"
                                        DisplayMemberBinding="{Binding DisplayDate}"
                                        Width="250"/>
                    </GridView>
                </ListView.View>

            </ListView>
        </Border>

        <!-- ACTION BUTTONS -->
        <StackPanel Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Margin="0,10,0,10">

            <Button Content="Add Sales Report"
            Style="{StaticResource NavButton}"
            Width="170"
            Height="40"
            Margin="10,0"
            Click="AddSalesReport_Click"/>

            <Button Content="Delete Sales Report"
            Style="{StaticResource NavButton}"
            Width="170"
            Height="40"
            Margin="10,0"
            Click="DeleteSalesReport_Click"/>

        </StackPanel>


    </Grid>
</UserControl>
